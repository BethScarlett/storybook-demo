import { StoryObj } from "@storybook/react";
import { Form } from "./Form";

const meta = {
  title: "Form",
  component: Form,
  parameters: {
    layout: "centered",
  },
  tags: ["autodocs"],
};

export default meta;
type Story = StoryObj<typeof meta>;
//If no input

//If invalid input

//If valid input
